import React from "react";
import "./Frame27.css";

function Frame27(props) {
  const { children, className } = props;

  return (
    <div className={`frame-632 ${className || ""}`}>
      <p className="text-78 roboto-normal-pale-sky-14px">{children}</p>
    </div>
  );
}

export default Frame27;
