import React from "react";
import "./Frame31.css";

function Frame31(props) {
  const { children, className } = props;

  return (
    <div className={`frame-71 ${className || ""}`}>
      <div className="e-waste-code-finder3 roboto-normal-pale-sky-14px">{children}</div>
    </div>
  );
}

export default Frame31;
